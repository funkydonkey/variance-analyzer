# Task 01: Базовый Streamlit UI для Variance Analyzer

## 🎯 Цель
Научиться работать со Streamlit и создать современный веб-интерфейс для чата с AI агентом.

## 🧠 Что ты узнаешь
- Основы Streamlit (компоненты, state management, layout)
- Работа с сессиями и chat UI
- Интеграция Streamlit с существующим Python кодом
- Best practices для Streamlit приложений

## ⏱️ Время выполнения
~2-3 часа (включая изучение документации)

---

## 📚 Часть 1: Изучи основы Streamlit (30 минут)

### Что такое Streamlit?
**Streamlit** - Python фреймворк для создания веб-приложений **БЕЗ HTML/CSS/JavaScript**.

**Философия:** Пишешь обычный Python скрипт → Streamlit превращает его в веб-app!

### Ключевые концепции

#### 1. Реактивность (Reactivity)
При каждом взаимодействии пользователя **весь скрипт перезапускается сверху вниз**:

```python
import streamlit as st

# Этот код выполняется КАЖДЫЙ РАЗ при взаимодействии
st.write("Hello World")

# Кнопка
if st.button("Click me"):
    st.write("Button clicked!")  # Показывается при клике
```

#### 2. Session State
Чтобы **сохранять данные** между перезапусками, используй `st.session_state`:

```python
# Инициализация (только при первом запуске)
if 'counter' not in st.session_state:
    st.session_state.counter = 0

# Использование
if st.button("Increment"):
    st.session_state.counter += 1

st.write(f"Counter: {st.session_state.counter}")
```

**Важно:** `st.session_state` - это словарь, который НЕ сбрасывается между перезапусками!

#### 3. Основные компоненты

| Компонент | Что делает | Пример |
|-----------|------------|--------|
| `st.title()` | Заголовок страницы | `st.title("My App")` |
| `st.write()` | Вывод текста/данных | `st.write("Hello")` |
| `st.text_input()` | Поле ввода | `name = st.text_input("Name")` |
| `st.button()` | Кнопка | `if st.button("Click"):` |
| `st.chat_message()` | Сообщение чата | `with st.chat_message("user"):` |
| `st.chat_input()` | Поле ввода для чата | `prompt = st.chat_input("Say...")` |

### 📖 Прочитай документацию (15 минут)

1. **Streamlit Chat Elements:**
   https://docs.streamlit.io/develop/api-reference/chat

2. **Session State Guide:**
   https://docs.streamlit.io/develop/concepts/architecture/session-state

3. **Быстрый туториал:**
   https://docs.streamlit.io/get-started/tutorials/create-a-multipage-app

---

## 🏗️ Часть 2: Структура проекта

### Создай новый файл `app_streamlit.py`

Уже существует файл `/Users/mo/claude_code/variance-analyzer/app_streamlit.py` - работай с ним!

### Структура приложения

```python
import streamlit as st
from ai.variance_agent import VarianceAnalyst

# 1. Конфигурация страницы (ВСЕГДА первая строка!)
st.set_page_config(...)

# 2. Инициализация session state
if 'messages' not in st.session_state:
    st.session_state.messages = []

# 3. UI компоненты
st.title("...")
sidebar = st.sidebar

# 4. Основная логика
# ... твой код ...
```

---

## ✍️ Часть 3: Реализация (ТЫ ПИШЕШЬ)

### Шаг 1: Конфигурация страницы

**Задача:** Настрой `st.set_page_config()` в самом начале файла.

```python
import streamlit as st

# ВСЕГДА первая строка!
st.set_page_config(
    page_title="TODO: Название",     # Название вкладки браузера
    page_icon="🤖",                   # Иконка
    layout="wide",                     # "wide" или "centered"
    initial_sidebar_state="expanded"   # Sidebar раскрыт по умолчанию
)
```

**Подсказки:**
- `page_title` - что видно в табе браузера
- `layout="wide"` - используй всю ширину экрана
- Документация: https://docs.streamlit.io/develop/api-reference/configuration/st.set_page_config

---

### Шаг 2: Инициализация Session State

**Задача:** Создай хранилище для истории чата.

```python
# Инициализация session state для истории сообщений
if 'messages' not in st.session_state:
    st.session_state.messages = []
    # TODO: Добавь приветственное сообщение от бота
    # Формат: {"role": "assistant", "content": "Привет! Я..."}

# Инициализация агента (создаётся только один раз!)
if 'analyst' not in st.session_state:
    # TODO: Создай VarianceAnalyst с файлом test_data.csv
    st.session_state.analyst = ...
```

**Подсказки:**
- `messages` - список словарей `[{"role": "user", "content": "..."}, ...]`
- `role` может быть `"user"` или `"assistant"`
- Агента создавай ОДИН РАЗ в session_state, а не при каждом запуске!

**Документация:**
- Session State: https://docs.streamlit.io/develop/api-reference/caching-and-state/st.session_state

---

### Шаг 3: Отображение истории чата

**Задача:** Покажи все сообщения из `st.session_state.messages`.

```python
# Отобразить историю чата
for message in st.session_state.messages:
    # TODO: Используй st.chat_message() для каждого сообщения
    # with st.chat_message(message["role"]):
    #     st.markdown(message["content"])
    pass
```

**Подсказки:**
- `st.chat_message(role)` - создаёт контейнер для сообщения
- `role` определяет иконку (👤 для user, 🤖 для assistant)
- `st.markdown()` - для отображения текста с форматированием
- **Важно:** Используй `with st.chat_message(...):` для правильного отображения

**Пример:**
```python
with st.chat_message("user"):
    st.markdown("Привет!")

with st.chat_message("assistant"):
    st.markdown("Здравствуй! 👋")
```

**Документация:**
- Chat Message: https://docs.streamlit.io/develop/api-reference/chat/st.chat_message

---

### Шаг 4: Поле ввода для нового сообщения

**Задача:** Добавь поле ввода внизу страницы.

```python
# Поле ввода для нового сообщения
if prompt := st.chat_input("Напиши свой вопрос о variance..."):
    # TODO:
    # 1. Добавь сообщение пользователя в st.session_state.messages
    # 2. Отобрази его через st.chat_message("user")
    # 3. Получи ответ от агента
    # 4. Добавь ответ в st.session_state.messages
    # 5. Отобрази ответ через st.chat_message("assistant")
    pass
```

**Подсказки:**
- `:=` - walrus operator, присваивает и проверяет одновременно
- `st.chat_input()` возвращает введённый текст или `None`
- После отправки сообщения Streamlit автоматически перезапустится!

**Псевдокод:**
```python
if prompt := st.chat_input("..."):
    # 1. Добавить в историю
    st.session_state.messages.append({"role": "user", "content": prompt})

    # 2. Показать сообщение пользователя
    with st.chat_message("user"):
        st.markdown(prompt)

    # 3. Получить ответ от агента
    with st.chat_message("assistant"):
        # TODO: Вызвать st.session_state.analyst.chat(prompt)
        # TODO: Показать ответ
        pass

    # 4. Добавить ответ в историю
    st.session_state.messages.append({"role": "assistant", "content": response})
```

**Документация:**
- Chat Input: https://docs.streamlit.io/develop/api-reference/chat/st.chat_input

---

### Шаг 5: Интеграция с AI агентом

**Задача:** Получи ответ от `VarianceAnalyst` и отобрази его.

```python
# Внутри блока "if prompt := st.chat_input(...):"

with st.chat_message("assistant"):
    # TODO: Используй st.spinner() для показа индикатора загрузки
    # with st.spinner("Анализирую данные..."):
    #     response = await st.session_state.analyst.chat(prompt)
    #     st.markdown(response)
    pass
```

**Проблема:** `VarianceAnalyst.chat()` - это **async** функция!

**Решение:** Streamlit поддерживает async/await!

```python
import asyncio

# Вариант 1: Прямой await (если весь скрипт async)
response = await st.session_state.analyst.chat(prompt)

# Вариант 2: asyncio.run() (если скрипт синхронный)
response = asyncio.run(st.session_state.analyst.chat(prompt))
```

**Подсказки:**
- `st.spinner(text)` - показывает анимацию загрузки
- Используй `asyncio.run()` если не хочешь делать весь скрипт async
- Ответ агента уже в Markdown формате - просто `st.markdown(response)`

**Документация:**
- Spinner: https://docs.streamlit.io/develop/api-reference/status/st.spinner

---

### Шаг 6: Sidebar с настройками (опционально)

**Задача:** Добавь боковую панель с информацией о проекте.

```python
# Sidebar
with st.sidebar:
    st.title("⚙️ Настройки")

    # TODO: Добавь информацию:
    # - Название проекта
    # - Используемый файл данных
    # - Кнопку "Очистить историю чата"
    # - Ссылки на документацию

    if st.button("🗑️ Очистить чат"):
        # TODO: Очисти st.session_state.messages
        # TODO: Добавь приветственное сообщение заново
        # st.rerun()  # Перезапустить приложение
        pass
```

**Подсказки:**
- `st.sidebar` или `with st.sidebar:` для контента в боковой панели
- `st.rerun()` - принудительно перезапускает приложение
- Можно добавить `st.divider()` для разделителей

**Документация:**
- Sidebar: https://docs.streamlit.io/develop/api-reference/layout/st.sidebar

---

## 📝 Полный шаблон файла

```python
"""
Streamlit UI для Variance Analyzer AI Chat.

Запуск:
    streamlit run app_streamlit.py
"""

import streamlit as st
import asyncio
from ai.variance_agent import VarianceAnalyst

# ========== 1. КОНФИГУРАЦИЯ ==========
st.set_page_config(
    page_title="TODO",
    page_icon="🤖",
    layout="wide",
    initial_sidebar_state="expanded"
)

# ========== 2. ИНИЦИАЛИЗАЦИЯ SESSION STATE ==========
if 'messages' not in st.session_state:
    # TODO: Инициализируй messages
    pass

if 'analyst' not in st.session_state:
    # TODO: Создай VarianceAnalyst
    pass

# ========== 3. ЗАГОЛОВОК ==========
st.title("TODO: Заголовок")
st.markdown("TODO: Описание")

# ========== 4. SIDEBAR ==========
with st.sidebar:
    # TODO: Настройки и информация
    pass

# ========== 5. ОТОБРАЖЕНИЕ ИСТОРИИ ЧАТА ==========
for message in st.session_state.messages:
    # TODO: Отобрази каждое сообщение
    pass

# ========== 6. ПОЛЕ ВВОДА ==========
if prompt := st.chat_input("Напиши свой вопрос..."):
    # TODO: Обработка нового сообщения
    pass
```

---

## 🚀 Запуск и тестирование

### 1. Установи Streamlit

```bash
cd /Users/mo/claude_code/variance-analyzer
pip install streamlit
```

### 2. Запусти приложение

```bash
streamlit run app_streamlit.py
```

### 3. Открой браузер

Streamlit автоматически откроет:
```
http://localhost:8501
```

### 4. Проверь функциональность

- [ ] Страница загружается без ошибок
- [ ] Приветственное сообщение показывается
- [ ] Можно отправить сообщение
- [ ] Агент отвечает
- [ ] История сообщений сохраняется
- [ ] Sidebar отображается
- [ ] Кнопка "Очистить чат" работает

---

## 🐛 Troubleshooting

### Ошибка: "This app has gone to sleep"
**Причина:** Streamlit Cloud бесплатный tier засыпает
**Решение:** Запускай локально или деплой на Streamlit Cloud

### Ошибка: "Cannot run async function"
**Причина:** Забыл `asyncio.run()`
**Решение:** Оберни вызов агента в `asyncio.run()`

### Ошибка: "RecursionError"
**Причина:** Бесконечный цикл перезапуска
**Решение:** Проверь что не меняешь `st.session_state` вне условий

### Сообщения дублируются
**Причина:** Добавляешь в историю ДО отображения
**Решение:** Сначала отобрази, потом добавь в историю

---

## 📊 Чеклист выполнения

### Обязательно:
- [ ] Установлен Streamlit
- [ ] `st.set_page_config()` настроен
- [ ] Session state инициализирован
- [ ] История чата отображается
- [ ] Поле ввода работает
- [ ] Агент отвечает на вопросы
- [ ] Ответы сохраняются в истории

### Опционально:
- [ ] Sidebar с настройками
- [ ] Кнопка очистки чата
- [ ] Spinner при загрузке
- [ ] Красивое форматирование Markdown
- [ ] Emoji в заголовках

---

## 🎓 Полезные ресурсы

### Документация
- **Streamlit Docs:** https://docs.streamlit.io
- **Chat Components:** https://docs.streamlit.io/develop/api-reference/chat
- **Session State:** https://docs.streamlit.io/develop/concepts/architecture/session-state

### Примеры
- **ChatGPT Clone:** https://github.com/streamlit/llm-examples/tree/main/Chatbot
- **Streamlit Gallery:** https://streamlit.io/gallery

### Видео
- **Official Streamlit Tutorial:** https://www.youtube.com/watch?v=R2nr1uZ8ffc
- **Build a ChatBot:** https://www.youtube.com/watch?v=sBhK-2K9bUc

---

## ❓ Если застрял - спрашивай!

**Полезные вопросы:**
- "Как правильно работать с async в Streamlit?"
- "Почему st.session_state не сохраняется?"
- "Как правильно очистить чат?"
- "Покажи пример работы с st.chat_message()"

**Когда закончишь:**
Напиши "готово" и покажи скриншот! Я дам фидбек и подскажу как улучшить 🎉

---

**Время начать!** ⏰ Открывай `app_streamlit.py` и начинай кодить! 🚀

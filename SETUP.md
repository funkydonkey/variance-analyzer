# SETUP.md - Инструкция по настройке проекта Variance Analyzer

## Предварительные требования

- Python 3.11 или выше
- **uv** - современный и быстрый пакетный менеджер (установим ниже)

---

## Шаг 0: Установка uv (если ещё не установлен)

### macOS/Linux:
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

### Проверка установки:
```bash
uv --version
```

Должно показать: `uv 0.5.x` или новее

**Примечание:** После установки может потребоваться перезапустить терминал или выполнить:
```bash
source $HOME/.local/bin/env
```

---

## Шаг 1: Создание структуры проекта

### Вариант А: Однострочная команда (быстрее)

```bash
mkdir -p core ai api ui tests/fixtures tasks && \
touch core/__init__.py core/models.py core/calculator.py core/loader.py core/filters.py \
      ai/__init__.py ai/agent.py ai/tools.py ai/prompts.py \
      api/__init__.py api/main.py api/routes.py api/schemas.py \
      tests/__init__.py tests/test_core.py tests/test_ai.py \
      .gitignore README.md
```

### Вариант Б: Пошаговые команды (нагляднее)

```bash
# Создать папки
mkdir -p core ai api ui tests/fixtures tasks

# Создать файлы core/
touch core/__init__.py core/models.py core/calculator.py core/loader.py core/filters.py

# Создать файлы ai/
touch ai/__init__.py ai/agent.py ai/tools.py ai/prompts.py

# Создать файлы api/
touch api/__init__.py api/main.py api/routes.py api/schemas.py

# Создать файлы tests/
touch tests/__init__.py tests/test_core.py tests/test_ai.py

# Создать конфиги
touch .gitignore README.md
```

### Проверка структуры

```bash
tree -L 2 -I '.venv|__pycache__|*.pyc'
```

Или если `tree` не установлен:

```bash
find . -type f -name "*.py" -o -name "*.toml" -o -name "*.md" | sort
```

Ожидаемая структура:

```
variance-analyzer/
├── core/
│   ├── __init__.py
│   ├── models.py
│   ├── calculator.py
│   ├── loader.py
│   └── filters.py
├── ai/
│   ├── __init__.py
│   ├── agent.py
│   ├── tools.py
│   └── prompts.py
├── api/
│   ├── __init__.py
│   ├── main.py
│   ├── routes.py
│   └── schemas.py
├── ui/
├── tests/
│   ├── __init__.py
│   ├── test_core.py
│   ├── test_ai.py
│   └── fixtures/
├── tasks/
├── .gitignore
├── README.md
├── CLAUDE.md
├── SETUP.md
└── tz.md
```

---

## Шаг 2: Инициализация проекта с uv

### 2.1 Инициализировать Python проект

```bash
# Находясь в корне проекта variance-analyzer/
pwd  # Должно показать путь до variance-analyzer

# Инициализация проекта (создаст pyproject.toml и .python-version)
uv init --no-readme
```

Эта команда создаст:
- `pyproject.toml` - конфигурация проекта и зависимостей
- `.python-version` - закрепит версию Python для проекта

### 2.2 Создать виртуальное окружение

```bash
# uv автоматически создаст .venv при первой установке зависимостей
# Но можно создать явно:
uv venv
```

---

## Шаг 3: Создать .gitignore

Открой файл `.gitignore` и вставь:

```gitignore
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
.venv

# uv
.python-version

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/

# Data files (uploaded by users)
data/
uploads/
*.csv
*.xlsx
*.xls

# Environment
.env
.env.local

# OS
.DS_Store
Thumbs.db

# Build
dist/
build/
*.egg-info/
```

---

## Шаг 4: Установить зависимости с uv

### 4.1 Добавить основные зависимости

```bash
# Core dependencies
uv add fastapi uvicorn[standard] pydantic python-multipart

# Data processing
uv add pandas openpyxl

# AI Agents
uv add openai

# Testing (как dev-зависимости)
uv add --dev pytest pytest-asyncio pytest-cov httpx
```

**Преимущество uv:** Все зависимости автоматически добавляются в `pyproject.toml`, установка в 10-100 раз быстрее чем pip!

### 4.2 Альтернатива: установить всё одной командой

```bash
uv add fastapi uvicorn[standard] pydantic python-multipart pandas openpyxl openai && \
uv add --dev pytest pytest-asyncio pytest-cov httpx
```

Установка займёт 10-30 секунд (вместо 1-2 минут с pip). Ты увидишь:
```
Resolved 50 packages in 1.2s
Installed 50 packages in 15s
```

---

## Шаг 5: Проверка установки

### 5.1 Проверить версию Python

```bash
uv run python --version
```

**Ожидается:** `Python 3.11.x` или `3.12.x` (должно быть >= 3.11)

### 5.2 Проверить установленные пакеты

```bash
uv pip list | grep -E "(fastapi|pandas|openai|pytest)"
```

**Ожидается:**
```
fastapi       0.115.x
openai        1.59.x
pandas        2.2.x
pytest        8.3.x
```

### 5.3 Проверить pytest

```bash
uv run pytest --version
```

**Ожидается:** `pytest 8.3.x`

### 5.4 Активировать окружение (опционально)

Можно работать через `uv run <команда>` или активировать окружение:

```bash
# Активация (для удобства)
source .venv/bin/activate

# Теперь можно использовать команды напрямую
python --version
pytest --version
```

**Проверка активации:** В терминале появится `(.venv)`:
```
(.venv) user@computer variance-analyzer %
```

---

## Чеклист самопроверки

Пройдись по чеклисту перед началом работы:

- [ ] **uv установлен:** команда `uv --version` работает
- [ ] **Структура создана:** команда `ls -la` показывает папки `core/`, `ai/`, `api/`, `ui/`, `tests/`, `tasks/`
- [ ] **Проект инициализирован:** файл `pyproject.toml` существует и содержит зависимости
- [ ] **Python >= 3.11:** команда `uv run python --version` показывает 3.11 или выше
- [ ] **Окружение создано:** папка `.venv/` существует
- [ ] **Зависимости установлены:** `uv pip list` содержит fastapi, pandas, openai, pytest
- [ ] **pytest работает:** команда `uv run pytest --version` выполняется без ошибок
- [ ] **.gitignore создан:** файл не пустой

---

## Частые проблемы и решения

### Проблема 1: "command not found: uv"
**Решение:**
```bash
# Переустанови uv
curl -LsSf https://astral.sh/uv/install.sh | sh

# Перезапусти терминал или обнови PATH
source $HOME/.local/bin/env
```

### Проблема 2: "Python version not compatible"
**Решение:**
```bash
# Проверь версию Python
python3 --version

# Если < 3.11, установи нужную версию и укажи явно:
uv venv --python 3.11
```

### Проблема 3: uv add выдаёт ошибки
**Решение:**
```bash
# Попробуй синхронизировать заново
uv sync

# Или пересоздай окружение
rm -rf .venv
uv venv
uv sync
```

### Проблема 4: tree команда не найдена
**Решение:**
```bash
# Установи tree (опционально)
# macOS:
brew install tree

# Или используй альтернативу:
find . -type d -maxdepth 2 | sort
```

---

## Когда закончишь настройку

Сообщи: **"окружение готово"**

Я проверю твою установку и создам **Task 01 Spec** - твоё первое задание! 🚀

---

## Полезные команды для работы с uv

```bash
# Запустить команду в окружении (БЕЗ активации)
uv run pytest
uv run python script.py
uv run uvicorn api.main:app --reload

# Добавить новый пакет
uv add package-name

# Добавить dev-зависимость
uv add --dev package-name

# Удалить пакет
uv remove package-name

# Синхронизировать зависимости (установить всё из pyproject.toml)
uv sync

# Обновить все пакеты
uv lock --upgrade

# Посмотреть установленные пакеты
uv pip list

# Активировать окружение вручную (опционально)
source .venv/bin/activate

# Деактивировать окружение
deactivate

# Запустить тесты (с активированным окружением)
pytest
# Или без активации:
uv run pytest

# Запустить тесты с покрытием
uv run pytest --cov=core --cov=ai --cov=api

# Запустить сервер (позже, когда будет готов API)
uv run uvicorn api.main:app --reload --port 8000
```

---

## Альтернативный метод: venv + pip (если не хочешь использовать uv)

<details>
<summary>Развернуть инструкции для venv + pip</summary>

### Создать виртуальное окружение

```bash
python3 -m venv venv
```

### Активировать окружение

```bash
# macOS/Linux:
source venv/bin/activate
```

### Создать requirements.txt

```txt
# Core dependencies
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.6
python-multipart==0.0.20

# Data processing
pandas==2.2.3
openpyxl==3.1.5

# AI Agents
openai==1.59.7

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx==0.28.1
```

### Установить зависимости

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

</details>

---

## Почему uv лучше?

- ⚡ **В 10-100 раз быстрее** pip
- 🔒 **Детерминированные сборки** через lock-файл
- 📦 **Единый pyproject.toml** вместо множества конфиг-файлов
- 🎯 **Упрощённое управление** версиями Python
- 🚀 **Современный стандарт** для Python проектов

---

**Удачи с настройкой! Если что-то непонятно - спрашивай!** 💪
